services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: health_coach
      POSTGRES_USER: health_coach
      POSTGRES_PASSWORD: health_coach
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
